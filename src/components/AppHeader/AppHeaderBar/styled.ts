import {styled} from "@mui/material";

export const StyledHeaderBar = styled("div")(() => ({
    display: "flex",
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
}));
