import { RADIUS_SIZES, SPACE_SIZES, TEXT_SIZES } from "../constants";

export const COMMON_OPTIONS = {
  radiusSize: {
    ...RADIUS_SIZES,
  },
  spaceSize: {
    ...SPACE_SIZES,
  },
  textSize: {
    ...TEXT_SIZES,
  },
};
